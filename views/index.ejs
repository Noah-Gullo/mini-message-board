<head>
    <div>
        <% messages.map((message) => { %>
            <p><%= message.user %></p>
            <p>"<%= message.text %>"</p>
            <p>Time Added: <%= message.added %> </p>
        <% }) %>
    </div>
</head>